logstashPipeline:
 logstash.conf: |
  input {
    rabbitmq {
      host => "rabbitmq.dev.svc"
      user => "user"
      password => "password"
      durable => true
    }
  }
  output {
    elasticsearch {
        hosts => "elasticsearch-master"
        data_stream => "true"
    }
  }
