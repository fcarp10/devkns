logstashPipeline:
 logstash.conf: |
  input {
    rabbitmq {
      host => "rabbitmq.dev.svc"
      durable => true
    }
  }
  output {
    elasticsearch {
        hosts => "elasticsearch-master"
        data_stream => "true"
    }
  }
